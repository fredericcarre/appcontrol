# AppControl Configuration
# Copy this file to .env and customize

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# PostgreSQL password (generate with: openssl rand -base64 24)
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_RANDOM_PASSWORD

# JWT signing secret (generate with: openssl rand -base64 48)
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_SECRET

# =============================================================================
# INITIAL ADMIN USER (created on first start)
# =============================================================================
SEED_ENABLED=true
SEED_ADMIN_EMAIL=admin@localhost
SEED_ADMIN_PASSWORD=admin
SEED_ADMIN_DISPLAY_NAME=Admin
SEED_ORG_NAME=Default Organization
SEED_ORG_SLUG=default

# =============================================================================
# SSO CONFIGURATION (optional)
# =============================================================================
# Local login (email/password) is always available.
# Configure SSO to add "Sign in with SSO" button.

# OIDC (set OIDC_DISCOVERY_URL to enable)
# OIDC_DISCOVERY_URL=https://login.example.com/.well-known/openid-configuration
# OIDC_CLIENT_ID=appcontrol
# OIDC_CLIENT_SECRET=your-client-secret
# OIDC_REDIRECT_URI=https://appcontrol.example.com/api/v1/auth/oidc/callback

# SAML (set SAML_IDP_SSO_URL to enable)
# SAML_IDP_SSO_URL=https://idp.example.com/sso
# SAML_IDP_CERT=/path/to/idp-cert.pem
# SAML_SP_ENTITY_ID=https://appcontrol.example.com

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# PostgreSQL
POSTGRES_DB=appcontrol
POSTGRES_USER=appcontrol
POSTGRES_PORT=5432

# Ports
BACKEND_PORT=3000
FRONTEND_PORT=8080
GATEWAY_PORT=4443

# CORS (comma-separated origins, e.g., https://appcontrol.example.com)
CORS_ORIGINS=

# Public URLs for enrollment commands (optional)
# If set, these URLs are shown in the UI for agent/gateway enrollment.
# Useful when agents connect via a different URL than the UI.
# PUBLIC_GATEWAY_URL=wss://gateway.example.com:8443
# PUBLIC_BACKEND_URL=wss://backend.example.com/ws/gateway

# JWT issuer name
JWT_ISSUER=appcontrol

# Logging level
RUST_LOG=info

# AppControl version (defaults to latest)
# APPCONTROL_VERSION=0.2.0
